<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context="com.birds.suni3.MainActivity">

    <item android:id="@+id/before"
        android:title="Before"
        android:icon="@drawable/btn_before"
        app:showAsAction="always"/>

    <item android:id="@+id/capture"
        android:title="Capture"
        android:icon="@drawable/btn_capture"
        app:showAsAction="always"/>

    <item android:id="@+id/add_course"
        android:title="@string/title_add_course"
        android:icon="@drawable/btn_schedule"
        app:showAsAction="always"/>

    <item android:id="@+id/save"
        android:title="@string/btn_save"
        app:showAsAction="always"/>

</menu>